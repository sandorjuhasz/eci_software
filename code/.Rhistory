iplot(est_did)
est_did = feols(log_nposts ~
i(WeekNumberf, ever_treated, 203)+ i(platform,MonthNumber)  |
WeekNumberf+platform+ as.factor(month),
df)#,cluster = c('MonthNumber'))
summary(est_did)
est_did$coeftable
d2.to_csv('results.csv')
d2<-est_did$coeftable
d2.to_csv('results.csv')
View(d2)
View(d2)
write.table(d2, file="coefficients.csv", sep = ",", col.names=NA)
write.table(d2, file="/Users/johanneswachs/Dropbox/majorprojects/so_gpt/data/coefficients.csv", sep = ",", col.names=NA)
est_did = feols(log_nposts ~
i(WeekNumber, ever_treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df)#,cluster = c('MonthNumber'))
summary(est_did)
d2<-est_did$coeftable
write.table(d2, file="/Users/johanneswachs/Dropbox/majorprojects/so_gpt/data/coefficients.csv", sep = ",", col.names=NA)
est_did = feols(log_nposts ~
i(WeekNumber, ever_treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df)#,cluster = c('MonthNumber'))
summary(est_did)
d2<-est_did$coeftable
write.table(d2, file="/Users/johanneswachs/Dropbox/majorprojects/so_gpt/data/coefficients.csv", sep = ",", col.names=NA)
est_did = feols(log_nposts ~
i(WeekNumber, ever_treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df,cluster = c('MonthNumber'))
summary(est_did)
d2<-est_did$coeftable
write.table(d2, file="/Users/johanneswachs/Dropbox/majorprojects/so_gpt/data/coefficients.csv", sep = ",", col.names=NA)
est_did = feols(log_nposts ~
i(WeekNumber, treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df,cluster = c('MonthNumber'))
est_did = feols(log_nposts ~
i(WeekNumberf, treated, 203)+ i(platform,MonthNumber)  |
WeekNumberf+platform+ as.factor(month),
df,cluster = c('MonthNumber'))
summary(est_did)
d2<-est_did$coeftable
write.table(d2, file="/Users/johanneswachs/Dropbox/majorprojects/so_gpt/data/coefficients.csv", sep = ",", col.names=NA)
iplot(est_did)
library(lmtest)
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df,
cluster = "MonthNumber")
library(lfe)
install.packages(lfe)
install.packages('lfe')
install.packages('lmtest')
library(lmtest)
library(lfe)
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df,
cluster = "MonthNumber")
cluster_se <- coeftest(model, vcov = vcovHC(model, cluster = "MonthNumber"))
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df,
cluster = "MonthNumber")
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df,
cluster = c("MonthNumber"))
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df)
cluster_se <- coeftest(model, vcov = vcovHC(model, cluster = "MonthNumber"))
#cluster = c("MonthNumber"))
model
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df,cluster = c("MonthNumber"))
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df,cluster = .~MonthNumber))
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df,cluster = .~MonthNumber)
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df,cluster = c("MonthNumber"))
model <- felm(log_nposts ~ treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df,cluster = c("MonthNumber"))
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
cluster_se <- coeftest(model, vcov = vcovHC(model, cluster = "MonthNumber"))
cluster_se <- coeftest(model, vcov = vcov(model, cluster = "MonthNumber"))
cluster_se
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumberf, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumberf + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
cluster_se <- coeftest(model, vcov = vcov(model, cluster = "MonthNumber"))
cluster_se
cluster_se
help(cluster_se)
??cluster_se
cluster_se
cluster_se
cluster_se$Estimate
cluster_se['Estimate']
cluster_se[]
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
cluster_se <- coeftest(model, vcov = vcov(model, cluster = "MonthNumber"))
cluster_se[]
model <- felm(log_nposts ~ ever_treated +
i(WeekNumberf, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
model <- felm(log_nposts ~
i(WeekNumberf, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
cluster_se <- coeftest(model, vcov = vcov(model, cluster = "MonthNumber"))
cluster_se[]
model <- felm(log_nposts ~
i(WeekNumberf, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumberf + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
cluster_se <- coeftest(model, vcov = vcov(model, cluster = "MonthNumber"))
cluster_se[]
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumberf, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumberf + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
model <- felm(log_nposts ~ post_gpt:ever_treated +
i(WeekNumberf, treated, 203) +
i(platform, MonthNumber) |
WeekNumberf + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
model <- felm(log_nposts ~ #post_gpt:ever_treated +
i(WeekNumberf, treated, 203) +
i(platform, MonthNumber) |
WeekNumberf + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
cluster_se <- coeftest(model, vcov = vcov(model, cluster = "MonthNumber"))
cluster_se[]
cluster_se[]
cluster_se[][0]
cluster_se[][1]
cluster_se[][:1]
cluster_se[1:]
cluster_se[1,
cluster_se[1,}
cluster_se[1,]
cluster_se[
cluster_se
cluster_se <- coeftest(model, vcov = vcov(model, cluster = "MonthNumber"))
cluster_se
cluster_se[]
model <- felm(log_nposts ~ #post_gpt:ever_treated +
i(WeekNumberf, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumberf + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
cluster_se <- coeftest(model, vcov = vcov(model, cluster = "MonthNumber"))
cluster_se[]
model <- felm(log_nposts ~ post_gpt+ever_treated +
i(WeekNumberf, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumberf + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
cluster_se <- coeftest(model, vcov = vcov(model, cluster = "MonthNumber"))
cluster_se[]
model <- felm(log_nposts ~ post_gpt+ever_treated +
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
cluster_se <- coeftest(model, vcov = vcov(model, cluster = "MonthNumber"))
cluster_se[]
model <- felm(log_nposts ~
i(WeekNumber, ever_treated, 203) +
i(platform, MonthNumber) |
WeekNumber + platform + as.factor(month),
data = df)#,cluster = c("MonthNumber"))
cluster_se <- coeftest(model, vcov = vcov(model, cluster = "MonthNumber"))
cluster_se[]
est_did = feols(log_nposts ~
i(WeekNumber, treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df,cluster = c('MonthNumber'))
summary(est_did)
est_did = feols(log_nposts ~
i(WeekNumber, treated, 2)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df,cluster = c('MonthNumber'))
summary(est_did)
est_did = feols(log_nposts ~ ever_treated+
i(WeekNumber, treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df,cluster = c('MonthNumber'))
est_did = feols(log_nposts ~ ever_treated+
i(WeekNumber, ever_treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df,cluster = c('MonthNumber'))
est_did = feols(log_nposts ~
i(WeekNumber, ever_treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df,cluster = c('MonthNumber'))
summary(est_did)
iplot(est_did)
df <-read.csv(
'/Users/johanneswachs/Dropbox/majorprojects/so_gpt/data/intermediate_regression_output.csv')
#treatment timing: @ 204 - so 203 is 0
est_did = feols(log_nposts ~
post_gpt+i(WeekNumber, ever_treated, 203)+ WeekNumber+platform,
df,cluster=c('MonthNumber'),panel.id = ~platform + WeekNumber)
library(fixest)
df <-read.csv(
'/Users/johanneswachs/Dropbox/majorprojects/so_gpt/data/intermediate_regression_output.csv')
#treatment timing: @ 204 - so 203 is 0
est_did = feols(log_nposts ~
post_gpt+i(WeekNumber, ever_treated, 203)+ WeekNumber+platform,
df,cluster=c('MonthNumber'),panel.id = ~platform + WeekNumber)
summary(est_did)
iplot(est_did)
post_gpt
post_gpt
#treatment timing: @ 204 - so 203 is 0
est_did = feols(log_nposts ~
post_gpt+i(WeekNumber, ever_treated, 203)+ WeekNumber+platform,
df,cluster=c('MonthNumber'),panel.id = ~platform + WeekNumber)
summary(est_did)
iplot(est_did)
df$post_gpt <- ifelse(df$post_gpt == "True", 1, 0)
df$treated <- df$ever_treated*df$post_gpt
#works!
est_did = feols(log_nposts ~ treated + i(platform,MonthNumber)  |
platform + WeekNumber + as.factor(month),
df,cluster = 'MonthNumber')
summary(est_did)
est_did
df$ihs <-asinh(df$Id)
est_did = feols(log_nposts ~ treated + i(platform,MonthNumber)  |
platform + WeekNumber + as.factor(month),
df,cluster = 'MonthNumber')
summary(est_did)
est_did
#latex output
etable(est_did, style.df = style.df(depvar.title = "", fixef.title = "",
fixef.suffix = " fixed effects", yesNo = "Yes"), tex = TRUE)
#treatment timing: @ 204 - so 203 is 0
est_did = feols(ihs ~
i(WeekNumber, treated, 203)+ WeekNumber+platform,
df,cluster=c('MonthNumber'),panel.id = ~platform + WeekNumber)
summary(est_did)
#treatment timing: @ 204 - so 203 is 0
est_did = feols(ihs ~
i(WeekNumber, treated, 203),
df,cluster=c('MonthNumber'),panel.id = ~platform + WeekNumber)
summary(est_did)
iplot(est_did)
#treatment timing: @ 204 - so 203 is 0
est_did = feols(ihs ~
i(WeekNumber, treated, 203) | platform + WeekNumber,
df,cluster=c('MonthNumber'),panel.id = ~platform + WeekNumber)
summary(est_did)
iplot(est_did)
#treatment timing: @ 204 - so 203 is 0
est_did = feols(ihs ~
i(WeekNumber, ever_treated, 203) | platform + WeekNumber,
df,cluster=c('MonthNumber'),panel.id = ~platform + WeekNumber)
summary(est_did)
iplot(est_did)
df$ihs <-asinh(df$Id)
df$MonthNumberf<-as.factor(df$MonthNumber)
df$WeekNumberf<-as.factor(df$WeekNumber)
#works!
est_did = feols(log_nposts ~ treated + i(platform,)  |
platform + WeekNumber + as.factor(month),
df,cluster = 'MonthNumber')
summary(est_did)
#works!
est_did = feols(log_nposts ~ treated + i(platform,WeekNumberf)  |
platform + WeekNumber + as.factor(month),
df,cluster = 'MonthNumber')
summary(est_did)
#works!
est_did = feols(log_nposts ~ treated + i(platform,WeekNumber)  |
platform + WeekNumber + as.factor(month),
df,cluster = 'MonthNumber')
summary(est_did)
#works!
est_did = feols(ihs ~ treated + i(platform,WeekNumber)  |
platform + WeekNumber + as.factor(month),
df,cluster = 'MonthNumber')
summary(est_did)
#works!
est_did = feols(ihs ~ treated + i(platform,WeekNumber)  |
platform + WeekNumber + month,
df,cluster = 'MonthNumber')
summary(est_did)
#works!
est_did = feols(ihs ~ treated + i(platform,WeekNumber)  |
platform + WeekNumber,
df,cluster = 'MonthNumber')
summary(est_did)
est_did
est_did = feols(log_nposts ~ treated + i(platform,MonthNumber)  |
platform + WeekNumber + as.factor(month),
df,cluster = 'MonthNumber')
summary(est_did)
est_did = feols(log_nposts ~ treated + i(platform,WeekNumber)  |
platform + WeekNumber + as.factor(month),
df,cluster = 'MonthNumber')
est_did = feols(log_nposts ~ treated + i(platform,MonthNumber)  |
platform + WeekNumber + as.factor(month),
df,cluster = 'MonthNumber')
summary(est_did)
est_did = feols(log_nposts ~
i(WeekNumber, ever_treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df,cluster = c('MonthNumber'))
summary(est_did)
iplot(est_did)
d2<-est_did$coeftable
est_did = feols(log_nposts ~
i(WeekNumberf, ever_treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df,cluster = c('MonthNumber'))
summary(est_did)
iplot(est_did)
est_did = feols(log_nposts ~
i(WeekNumberf, ever_treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df)#,cluster = c('MonthNumber'))
summary(est_did)
iplot(est_did)
iplot(est_did,i=203)
iplot(est_did)
est_did = feols(log_nposts ~
i(WeekNumberf, ever_treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform,
df)#,cluster = c('MonthNumber'))
summary(est_did)
iplot(est_did)
est_did = feols(log_nposts ~
i(WeekNumberf, ever_treated, 203)  |
WeekNumber+platform+ as.factor(month),
df)#,cluster = c('MonthNumber'))
summary(est_did)
iplot(est_did)
est_did = feols(log_nposts ~
i(WeekNumberf, ever_treated, 203)+ i(platform,MonthNumber)  |
WeekNumber+platform+ as.factor(month),
df)#,cluster = c('MonthNumber'))
summary(est_did)
tail(summary(est_did))
library(EconGeo)
library(data.table)
library(dplyr)
library(igraph)
library(mefa4)
library(stargazer)
# software space to relatedness matrix
ssel <- fread("../outputs/software_space_edgelist.csv") %>% select(-drop)
ssel$proximity <- 1
# software space to relatedness matrix
ssel <- fread(
"/Users/johanneswachs/Dropbox/majorprojects/empirical_software/github_innovation_graph/
git_complexity/outputs/software_space_edgelist.csv") %>% select(-drop)
# software space to relatedness matrix
ssel <- fread(
"/Users/johanneswachs/Dropbox/majorprojects/empirical_software/github_innovation_graph/git_complexity/outputs/software_space_edgelist.csv") %>% select(-drop)
ssel$proximity <- 1
relatedness <- as.matrix(get.adjacency(graph.data.frame(ssel)))
# complexity table to Mcp matrix
cdf <- fread("../outputs/complexity_table2020.csv")
# complexity table to Mcp matrix
cdf <- fread(
"/Users/johanneswachs/Dropbox/majorprojects/empirical_software/github_innovation_graph/git_complexity/outputs/complexity_table2020.csv")
cdf$rca01 <- ifelse(cdf$rca >= 1, 1, 0)
mcp <- select(cdf, iso2_code, language, rca01)
mcp_mat <- EconGeo::get.matrix(mcp)
# relatedness density
rel_density <- relatedness.density(mcp_mat, relatedness)
library(EconGeo)
library(data.table)
library(dplyr)
library(igraph)
library(mefa4)
library(stargazer)
# software space to relatedness matrix
ssel <- fread(
"/Users/johanneswachs/Dropbox/majorprojects/empirical_software/github_innovation_graph/git_complexity/outputs/software_space_edgelist.csv") %>% select(-drop)
ssel$proximity <- 1
relatedness <- as.matrix(get.adjacency(graph.data.frame(ssel)))
# complexity table to Mcp matrix
cdf <- fread(
"/Users/johanneswachs/Dropbox/majorprojects/empirical_software/github_innovation_graph/git_complexity/outputs/complexity_table2020.csv")
cdf$rca01 <- ifelse(cdf$rca >= 1, 1, 0)
mcp <- select(cdf, iso2_code, language, rca01)
mcp_mat <- EconGeo::get.matrix(mcp)
# relatedness density
rel_density <- relatedness.density(mcp_mat, relatedness)
mcp_mat <- get.matrix(mcp)
mcp_mat <- EconGeo::get.matrix(mcp)
# relatedness density
rel_density <- relatedness.density(mcp_mat, relatedness)
rel_density <- data.table(Melt(rel_density))
colnames(rel_density) <- c("country", "language", "rel_density")
library(EconGeo)
library(data.table)
library(dplyr)
library(igraph)
library(mefa4)
library(stargazer)
library(fixest)
setwd('/Users/johanneswachs/Dropbox/majorprojects/empirical_software/github_innovation_graph/git_complexity/code/')
fdf=fread('../outputs/relatedness_country_semester_panel_with_collab_neighbors.csv')
head(fdf)
m1<-feols(entry010 ~ rel_density | iso2_code +language + semester_id,
panel.id=~iso2_code+semester_id,
data=fdf,
se="DK")
m2<-feols(entry010 ~ neighbor_related | iso2_code +language + semester_id,
panel.id=~iso2_code+semester_id,
data=fdf,
se="DK")
m3<-feols(entry010 ~ rel_density+neighbor_related | iso2_code +language + semester_id,
panel.id=~iso2_code+semester_id,
data=fdf,
se="DK")
m4<-feols(entry010 ~ rel_density+neighbor_related + pci | iso2_code + semester_id,
panel.id=~iso2_code+semester_id,
data=fdf,
se="DK")
m5<-feols(entry010 ~ rel_density+neighbor_related+rel_density:neighbor_related | iso2_code +language + semester_id,
panel.id=~iso2_code+semester_id,
data=fdf,
se="DK")
m6<-feols(entry010 ~ rel_density+neighbor_related+rel_density:neighbor_related +pci | semester_id,
panel.id=~iso2_code+semester_id,
data=fdf,
se = 'twoway')
etable(m1,m2,m3,m4,m5,m6)
etable(m1,m2,m3,m4,m5,m6,
file = '../outputs/interaction_model_v2.tex')
m21<-feols(entry011 ~ rel_density | iso2_code +language + semester_id,
panel.id=~iso2_code+semester_id,
data=fdf,
se="DK")
m22<-feols(entry011 ~ neighbor_related | iso2_code +language + semester_id,
panel.id=~iso2_code+semester_id,
data=fdf,
se="DK")
m23<-feols(entry011 ~ rel_density+neighbor_related | iso2_code +language + semester_id,
panel.id=~iso2_code+semester_id,
data=fdf,
se="DK")
m24<-feols(entry011 ~ rel_density+neighbor_related + pci | iso2_code + semester_id,
panel.id=~iso2_code+semester_id,
data=fdf,
se="DK")
m25<-feols(entry011 ~ rel_density+neighbor_related+rel_density:neighbor_related | iso2_code +language + semester_id,
panel.id=~iso2_code+semester_id,
data=fdf,
se="DK")
m26<-feols(entry011 ~ rel_density+neighbor_related+rel_density:neighbor_related +pci | iso2_code + semester_id,
panel.id=~iso2_code+semester_id,
data=fdf,
se="DK")
etable(m21,m22,m23,m24,m25,m26)
fdf
7 * 150 * 180
